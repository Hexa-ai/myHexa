# P2 — DB Evolution + Auth Membres Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Évolution du schéma `recipients` pour accueillir l'auth Supabase, mise en place des policies RLS multi-tenant, et création de la page `/login` + `/admin/devices` (read-only) pour valider l'isolation.

**Architecture :** Migration Postgres ajoute `auth_user_id` + `role` sur `recipients`. Trigger DB lie automatiquement `auth.users` ↔ `recipients` par matching email. Policies RLS sur 4 tables (`companies`, `recipients`, `devices`, `reports`) filtrent par `company_id` du recipient lié à `auth.uid()`. Côté Vue : `authStore` Pinia + router guard + LoginView + AdminLayout + DevicesView listant les devices visibles via RLS.

**Tech Stack :** Supabase migrations SQL + Supabase Auth (email+password) + Pinia + Vue Router + shadcn-vue (Card, Form, Input, Button, Table).

**Prérequis :**
- P1 terminé (app déployée, CI verte)
- Accès au projet Supabase (CLI configurée OU MCP supabase actif)
- Au moins un destinataire test en base avec un email contrôlable

---

## File Structure

```
supabase/migrations/
├── 20260514100000_add_auth_to_recipients.sql       ← nouveau
├── 20260514100100_recipients_auth_link_trigger.sql ← nouveau
└── 20260514100200_rls_policies_tenant.sql          ← nouveau

app/src/
├── stores/auth.ts                ← réécrit (skeleton → réel)
├── router/index.ts               ← modifié (ajout routes, guards)
├── router/guards.ts              ← nouveau
├── lib/supabase.ts               ← inchangé (déjà OK depuis P1)
├── views/
│   ├── auth/LoginView.vue        ← nouveau
│   └── admin/
│       ├── AdminLayout.vue       ← nouveau
│       └── DevicesView.vue       ← nouveau (read-only)
├── composables/
│   └── useDevices.ts             ← nouveau (read-only, list)
└── types/supabase.ts             ← nouveau (généré CLI)
```

---

### Task 1 : Migration — ajouter `auth_user_id` et `role` à `recipients`

**Files:**
- Create: `supabase/migrations/20260514100000_add_auth_to_recipients.sql`

- [ ] **Step 1.1 : Écrire la migration**

Créer le fichier :

```sql
-- Lier les destinataires à des comptes Supabase Auth
ALTER TABLE recipients
  ADD COLUMN auth_user_id uuid REFERENCES auth.users(id) ON DELETE SET NULL,
  ADD COLUMN role text NOT NULL DEFAULT 'external'
    CHECK (role IN ('member', 'external'));

-- Un member doit avoir un compte Auth lié
ALTER TABLE recipients
  ADD CONSTRAINT recipients_member_has_auth
    CHECK (role = 'external' OR auth_user_id IS NOT NULL);

CREATE INDEX recipients_auth_user_id_idx ON recipients (auth_user_id);

COMMENT ON COLUMN recipients.role IS
  'external = reçoit emails uniquement ; member = peut se logger via Supabase Auth';
COMMENT ON COLUMN recipients.auth_user_id IS
  'Lien vers auth.users si le destinataire a un compte ; NULL pour les externes';
```

- [ ] **Step 1.2 : Appliquer la migration**

Via MCP :
```
mcp__supabase__apply_migration(name="add_auth_to_recipients", query=<contenu du fichier>)
```

Ou via CLI :
```bash
supabase db push
```

- [ ] **Step 1.3 : Vérifier**

Via MCP `mcp__supabase__list_tables` (filtrer sur `recipients`) ou CLI :
```bash
supabase db pull --schema public
```

Attendu : `recipients` a maintenant `auth_user_id` et `role`.

- [ ] **Step 1.4 : Commit**

```bash
git add supabase/migrations/20260514100000_add_auth_to_recipients.sql
git commit -m "feat(p2): add auth_user_id and role to recipients"
```

---

### Task 2 : Migration — trigger de liaison auto auth.users ↔ recipients

**Files:**
- Create: `supabase/migrations/20260514100100_recipients_auth_link_trigger.sql`

- [ ] **Step 2.1 : Écrire la migration**

```sql
-- Quand un user Supabase Auth est créé, on lie automatiquement le recipient
-- existant qui a le même email (pour qu'il devienne 'member' au premier login)
CREATE OR REPLACE FUNCTION link_recipient_to_auth_user()
RETURNS trigger
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
BEGIN
  UPDATE recipients
  SET auth_user_id = NEW.id,
      role         = 'member',
      updated_at   = now()
  WHERE contact_email = NEW.email
    AND auth_user_id IS NULL;
  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW
  EXECUTE FUNCTION link_recipient_to_auth_user();

COMMENT ON FUNCTION link_recipient_to_auth_user IS
  'Match auth.users.email ↔ recipients.contact_email au signup, promeut en member';
```

- [ ] **Step 2.2 : Appliquer**

```
mcp__supabase__apply_migration(name="recipients_auth_link_trigger", query=<contenu>)
```

- [ ] **Step 2.3 : Test manuel**

1. Créer un recipient en base avec `contact_email = 'test-link@example.com'` (via dashboard ou SQL)
2. Créer un user dans Supabase Auth avec le même email (dashboard → Authentication → Add user)
3. Vérifier que `recipients.auth_user_id` et `role` ont été mis à jour automatiquement

SQL de vérif :
```sql
SELECT id, contact_email, role, auth_user_id FROM recipients WHERE contact_email = 'test-link@example.com';
```

Attendu : `role = 'member'`, `auth_user_id` non NULL.

- [ ] **Step 2.4 : Commit**

```bash
git add supabase/migrations/20260514100100_recipients_auth_link_trigger.sql
git commit -m "feat(p2): trigger to link auth.users to recipients by email"
```

---

### Task 3 : Migration — Policies RLS multi-tenant

**Files:**
- Create: `supabase/migrations/20260514100200_rls_policies_tenant.sql`

- [ ] **Step 3.1 : Écrire la migration**

```sql
-- Fonction helper : retourne la company_id du recipient lié à auth.uid()
CREATE OR REPLACE FUNCTION current_recipient_company_id()
RETURNS uuid
LANGUAGE sql
STABLE
SECURITY DEFINER
SET search_path = public
AS $$
  SELECT company_id FROM recipients
  WHERE auth_user_id = auth.uid()
  LIMIT 1
$$;

COMMENT ON FUNCTION current_recipient_company_id IS
  'Renvoie la company_id du recipient connecté ; NULL si pas authentifié';

-- COMPANIES : un member voit uniquement sa company
CREATE POLICY companies_select_own
  ON companies FOR SELECT
  USING (id = current_recipient_company_id());

-- RECIPIENTS : un member voit les recipients de sa company
CREATE POLICY recipients_select_own_company
  ON recipients FOR SELECT
  USING (company_id = current_recipient_company_id());

CREATE POLICY recipients_insert_own_company
  ON recipients FOR INSERT
  WITH CHECK (company_id = current_recipient_company_id());

CREATE POLICY recipients_update_own_company
  ON recipients FOR UPDATE
  USING (company_id = current_recipient_company_id())
  WITH CHECK (company_id = current_recipient_company_id());

CREATE POLICY recipients_delete_own_company
  ON recipients FOR DELETE
  USING (company_id = current_recipient_company_id());

-- DEVICES : idem
CREATE POLICY devices_select_own_company
  ON devices FOR SELECT
  USING (company_id = current_recipient_company_id());

CREATE POLICY devices_update_own_company
  ON devices FOR UPDATE
  USING (company_id = current_recipient_company_id())
  WITH CHECK (company_id = current_recipient_company_id());

-- REPORTS : un member voit les reports de devices de sa company
CREATE POLICY reports_select_own_company
  ON reports FOR SELECT
  USING (
    device_id IN (
      SELECT id FROM devices WHERE company_id = current_recipient_company_id()
    )
  );
```

**Note :** on n'active pas d'INSERT/DELETE sur `devices` et `reports` côté admin pour le moment — ces tables sont écrites par n8n. On ajoutera des policies INSERT/DELETE quand on portera ces flux.

`ALTER TABLE ... ENABLE ROW LEVEL SECURITY` est déjà fait dans la migration init (`20260419115620_init_haios.sql`). Donc les policies prennent effet immédiatement.

**Important** : `ENABLE ROW LEVEL SECURITY` par défaut bloque les accès `service_role` non plus que pas — c'est l'inverse : `service_role` bypasse toujours RLS. Donc n8n (qui utilise `service_role`) continue de fonctionner.

- [ ] **Step 3.2 : Appliquer**

```
mcp__supabase__apply_migration(name="rls_policies_tenant", query=<contenu>)
```

- [ ] **Step 3.3 : Test SQL d'isolation**

Avec un user de test logué (récupérer son JWT via la page de login après Task 7), tester via le dashboard SQL avec `set local role authenticated` et `set local request.jwt.claims = '...'`. Plus simplement : tester via l'app Vue à la Task 9.

- [ ] **Step 3.4 : Vérifier les advisors Supabase**

```
mcp__supabase__get_advisors(type="security")
```

Attendu : aucun avertissement critique. Si "RLS disabled on table X" apparaît, c'est qu'on a oublié `ENABLE ROW LEVEL SECURITY` sur cette table — corriger.

- [ ] **Step 3.5 : Commit**

```bash
git add supabase/migrations/20260514100200_rls_policies_tenant.sql
git commit -m "feat(p2): RLS policies for tenant isolation"
```

---

### Task 4 : Générer les types TypeScript Supabase

**Files:**
- Create: `app/src/types/supabase.ts`

- [ ] **Step 4.1 : Générer**

Via MCP :
```
mcp__supabase__generate_typescript_types()
```

Sauvegarder le résultat dans `app/src/types/supabase.ts`.

Ou via CLI (si projet linké) :
```bash
supabase gen types typescript --linked > app/src/types/supabase.ts
```

- [ ] **Step 4.2 : Ajouter un script `package.json`**

Dans `app/package.json`, ajouter :

```json
{
  "scripts": {
    "types:gen": "supabase gen types typescript --linked > src/types/supabase.ts"
  }
}
```

- [ ] **Step 4.3 : Typer le client Supabase**

Modifier `app/src/lib/supabase.ts` :

```ts
import { createClient } from '@supabase/supabase-js'
import type { Database } from '@/types/supabase'

const url = import.meta.env.VITE_SUPABASE_URL
const anonKey = import.meta.env.VITE_SUPABASE_ANON_KEY

if (!url || !anonKey) {
  throw new Error('Missing Supabase env vars')
}

export const supabase = createClient<Database>(url, anonKey)
```

- [ ] **Step 4.4 : Vérifier typecheck**

```bash
cd app && pnpm typecheck
```

Attendu : aucune erreur.

- [ ] **Step 4.5 : Commit**

```bash
git add -A
git commit -m "feat(p2): generate and type Supabase TS types"
```

---

### Task 5 : Réécrire l'authStore

**Files:**
- Modify: `app/src/stores/auth.ts`

- [ ] **Step 5.1 : Test unitaire**

Créer `app/tests/unit/stores/auth.spec.ts` :

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { setActivePinia, createPinia } from 'pinia'
import { useAuthStore } from '@/stores/auth'

vi.mock('@/lib/supabase', () => ({
  supabase: {
    auth: {
      signInWithPassword: vi.fn(),
      signOut: vi.fn(),
      getSession: vi.fn(() => ({ data: { session: null } })),
      onAuthStateChange: vi.fn(),
    },
    from: vi.fn(),
  },
}))

describe('authStore', () => {
  beforeEach(() => setActivePinia(createPinia()))

  it('starts unauthenticated', () => {
    const store = useAuthStore()
    expect(store.isAuthenticated).toBe(false)
    expect(store.recipient).toBeNull()
  })
})
```

- [ ] **Step 5.2 : Lancer le test (FAIL attendu)**

```bash
cd app && pnpm test:unit
```

Attendu : test échoue parce que le store actuel n'expose pas `isAuthenticated`.

- [ ] **Step 5.3 : Écrire le store**

Remplacer `app/src/stores/auth.ts` :

```ts
import { defineStore } from 'pinia'
import { computed, ref } from 'vue'
import type { Session } from '@supabase/supabase-js'
import { supabase } from '@/lib/supabase'
import type { Database } from '@/types/supabase'

type Recipient = Database['public']['Tables']['recipients']['Row']

export const useAuthStore = defineStore('auth', () => {
  const session = ref<Session | null>(null)
  const recipient = ref<Recipient | null>(null)
  const loading = ref(false)
  const error = ref<string | null>(null)

  const isAuthenticated = computed(() => session.value !== null)
  const companyId = computed(() => recipient.value?.company_id ?? null)

  async function init() {
    const { data } = await supabase.auth.getSession()
    session.value = data.session
    if (session.value) {
      await loadRecipient()
    }
    supabase.auth.onAuthStateChange(async (_event, newSession) => {
      session.value = newSession
      if (newSession) await loadRecipient()
      else recipient.value = null
    })
  }

  async function loadRecipient() {
    if (!session.value) return
    const { data, error: err } = await supabase
      .from('recipients')
      .select('*')
      .eq('auth_user_id', session.value.user.id)
      .maybeSingle()
    if (err) {
      error.value = err.message
      return
    }
    recipient.value = data
  }

  async function signIn(email: string, password: string) {
    loading.value = true
    error.value = null
    const { error: err } = await supabase.auth.signInWithPassword({ email, password })
    loading.value = false
    if (err) {
      error.value = err.message
      return false
    }
    return true
  }

  async function signOut() {
    await supabase.auth.signOut()
    session.value = null
    recipient.value = null
  }

  return {
    session,
    recipient,
    loading,
    error,
    isAuthenticated,
    companyId,
    init,
    signIn,
    signOut,
  }
})
```

- [ ] **Step 5.4 : Lancer le test (PASS attendu)**

```bash
cd app && pnpm test:unit
```

Attendu : test passe.

- [ ] **Step 5.5 : Initialiser le store au démarrage**

Modifier `app/src/main.ts` pour appeler `init()` :

```ts
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import { router } from '@/router'
import { useAuthStore } from '@/stores/auth'
import App from './App.vue'
import './style.css'

const app = createApp(App)
const pinia = createPinia()
app.use(pinia)
app.use(router)

const auth = useAuthStore()
auth.init().finally(() => app.mount('#app'))
```

- [ ] **Step 5.6 : Commit**

```bash
git add -A
git commit -m "feat(p2): implement authStore with Supabase Auth"
```

---

### Task 6 : Router guards

**Files:**
- Create: `app/src/router/guards.ts`
- Modify: `app/src/router/index.ts`

- [ ] **Step 6.1 : Créer le guard**

Créer `app/src/router/guards.ts` :

```ts
import type { NavigationGuardWithThis } from 'vue-router'
import { useAuthStore } from '@/stores/auth'

export const requireAuth: NavigationGuardWithThis<undefined> = (to) => {
  const auth = useAuthStore()
  if (!auth.isAuthenticated) {
    return { name: 'login', query: { redirect: to.fullPath } }
  }
}
```

- [ ] **Step 6.2 : Modifier le router**

Remplacer `app/src/router/index.ts` :

```ts
import { createRouter, createWebHistory } from 'vue-router'
import HomeView from '@/views/HomeView.vue'
import LoginView from '@/views/auth/LoginView.vue'
import AdminLayout from '@/views/admin/AdminLayout.vue'
import DevicesView from '@/views/admin/DevicesView.vue'
import { requireAuth } from '@/router/guards'

export const router = createRouter({
  history: createWebHistory(),
  routes: [
    { path: '/', name: 'home', component: HomeView },
    { path: '/login', name: 'login', component: LoginView },
    {
      path: '/admin',
      component: AdminLayout,
      beforeEnter: requireAuth,
      children: [
        { path: 'devices', name: 'admin-devices', component: DevicesView },
        { path: '', redirect: { name: 'admin-devices' } },
      ],
    },
  ],
})
```

- [ ] **Step 6.3 : Commit (build cassé temporairement, c'est normal — fixé par Tasks 7-8)**

```bash
git add -A
git commit -m "feat(p2): add router guards and admin routes"
```

---

### Task 7 : LoginView

**Files:**
- Create: `app/src/views/auth/LoginView.vue`

- [ ] **Step 7.1 : Ajouter les composants shadcn nécessaires**

```bash
cd app && pnpm dlx shadcn-vue@latest add input label card
```

- [ ] **Step 7.2 : Écrire la vue**

Créer `app/src/views/auth/LoginView.vue` :

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { useRouter, useRoute } from 'vue-router'
import { useAuthStore } from '@/stores/auth'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

const auth = useAuthStore()
const router = useRouter()
const route = useRoute()

const email = ref('')
const password = ref('')

async function handleSubmit() {
  const ok = await auth.signIn(email.value, password.value)
  if (!ok) return
  const redirect = (route.query.redirect as string) || '/admin'
  router.push(redirect)
}
</script>

<template>
  <main class="min-h-screen flex items-center justify-center p-4 bg-slate-50">
    <Card class="w-full max-w-md">
      <CardHeader>
        <CardTitle>Connexion myHexa</CardTitle>
      </CardHeader>
      <CardContent>
        <form class="space-y-4" @submit.prevent="handleSubmit">
          <div class="space-y-2">
            <Label for="email">Email</Label>
            <Input id="email" v-model="email" type="email" required autocomplete="email" />
          </div>
          <div class="space-y-2">
            <Label for="password">Mot de passe</Label>
            <Input
              id="password"
              v-model="password"
              type="password"
              required
              autocomplete="current-password"
            />
          </div>
          <p v-if="auth.error" class="text-sm text-red-600">{{ auth.error }}</p>
          <Button type="submit" class="w-full" :disabled="auth.loading">
            {{ auth.loading ? 'Connexion…' : 'Se connecter' }}
          </Button>
        </form>
      </CardContent>
    </Card>
  </main>
</template>
```

- [ ] **Step 7.3 : Vérifier visuellement**

```bash
pnpm dev
```

Aller sur `http://localhost:5173/login`, vérifier l'apparence du formulaire. Pas besoin de tester le login fonctionnel ici, ce sera fait à la Task 9.

- [ ] **Step 7.4 : Commit**

```bash
git add -A
git commit -m "feat(p2): add LoginView with email+password form"
```

---

### Task 8 : AdminLayout

**Files:**
- Create: `app/src/views/admin/AdminLayout.vue`

- [ ] **Step 8.1 : Écrire le layout**

Créer `app/src/views/admin/AdminLayout.vue` :

```vue
<script setup lang="ts">
import { RouterView, RouterLink, useRouter } from 'vue-router'
import { useAuthStore } from '@/stores/auth'
import { Button } from '@/components/ui/button'

const auth = useAuthStore()
const router = useRouter()

async function handleLogout() {
  await auth.signOut()
  router.push({ name: 'login' })
}
</script>

<template>
  <div class="min-h-screen flex">
    <aside class="w-64 bg-slate-900 text-slate-100 p-4 flex flex-col">
      <h1 class="text-xl font-bold mb-6">myHexa Admin</h1>
      <nav class="flex-1 space-y-1">
        <RouterLink
          :to="{ name: 'admin-devices' }"
          class="block px-3 py-2 rounded hover:bg-slate-800"
          active-class="bg-slate-800"
        >
          Devices
        </RouterLink>
      </nav>
      <div class="text-xs text-slate-400 mb-2 truncate">
        {{ auth.recipient?.contact_email }}
      </div>
      <Button variant="secondary" size="sm" @click="handleLogout">Se déconnecter</Button>
    </aside>
    <main class="flex-1 p-8 bg-slate-50">
      <RouterView />
    </main>
  </div>
</template>
```

- [ ] **Step 8.2 : Commit**

```bash
git add app/src/views/admin/AdminLayout.vue
git commit -m "feat(p2): add AdminLayout shell with sidebar"
```

---

### Task 9 : DevicesView (read-only)

**Files:**
- Create: `app/src/composables/useDevices.ts`, `app/src/views/admin/DevicesView.vue`

- [ ] **Step 9.1 : Test du composable**

Créer `app/tests/unit/composables/useDevices.spec.ts` :

```ts
import { describe, it, expect, vi } from 'vitest'
import { setActivePinia, createPinia } from 'pinia'

const mockSelect = vi.fn()
vi.mock('@/lib/supabase', () => ({
  supabase: { from: vi.fn(() => ({ select: () => mockSelect() })) },
}))

import { useDevices } from '@/composables/useDevices'

describe('useDevices', () => {
  it('loads devices into a ref', async () => {
    setActivePinia(createPinia())
    mockSelect.mockResolvedValue({
      data: [{ id: '1', name: 'D1' }],
      error: null,
    })
    const { devices, load } = useDevices()
    await load()
    expect(devices.value).toHaveLength(1)
  })
})
```

- [ ] **Step 9.2 : Lancer (FAIL)**

```bash
cd app && pnpm test:unit
```

Attendu : module `@/composables/useDevices` non trouvé.

- [ ] **Step 9.3 : Implémenter le composable**

Créer `app/src/composables/useDevices.ts` :

```ts
import { ref } from 'vue'
import { supabase } from '@/lib/supabase'
import type { Database } from '@/types/supabase'

type Device = Database['public']['Tables']['devices']['Row']

export function useDevices() {
  const devices = ref<Device[]>([])
  const loading = ref(false)
  const error = ref<string | null>(null)

  async function load() {
    loading.value = true
    error.value = null
    const { data, error: err } = await supabase
      .from('devices')
      .select('*')
      .order('name', { ascending: true })
    loading.value = false
    if (err) {
      error.value = err.message
      return
    }
    devices.value = data ?? []
  }

  return { devices, loading, error, load }
}
```

- [ ] **Step 9.4 : Lancer (PASS)**

```bash
pnpm test:unit
```

Attendu : tous les tests passent.

- [ ] **Step 9.5 : Ajouter le composant Table**

```bash
cd app && pnpm dlx shadcn-vue@latest add table
```

- [ ] **Step 9.6 : Écrire la vue**

Créer `app/src/views/admin/DevicesView.vue` :

```vue
<script setup lang="ts">
import { onMounted } from 'vue'
import { useDevices } from '@/composables/useDevices'
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from '@/components/ui/table'

const { devices, loading, error, load } = useDevices()

onMounted(load)
</script>

<template>
  <section>
    <h2 class="text-2xl font-bold mb-6">Devices</h2>

    <p v-if="loading">Chargement…</p>
    <p v-else-if="error" class="text-red-600">{{ error }}</p>

    <Table v-else>
      <TableHeader>
        <TableRow>
          <TableHead>Nom</TableHead>
          <TableHead>Numéro de série</TableHead>
          <TableHead>Dernière connexion</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        <TableRow v-for="device in devices" :key="device.id">
          <TableCell>{{ device.name }}</TableCell>
          <TableCell>{{ device.serial_number || '—' }}</TableCell>
          <TableCell>{{ device.last_connection_at || '—' }}</TableCell>
        </TableRow>
        <TableRow v-if="devices.length === 0">
          <TableCell colspan="3" class="text-center text-slate-500">
            Aucun device pour votre entreprise.
          </TableCell>
        </TableRow>
      </TableBody>
    </Table>
  </section>
</template>
```

- [ ] **Step 9.7 : Commit**

```bash
git add -A
git commit -m "feat(p2): add DevicesView (read-only) with useDevices composable"
```

---

### Task 10 : Test manuel d'isolation multi-tenant

- [ ] **Step 10.1 : Préparer 2 companies de test en base**

Via SQL ou dashboard :
- Company A avec 2 devices et 1 recipient `member` (email `member-a@test.com`)
- Company B avec 3 devices et 1 recipient `member` (email `member-b@test.com`)

Créer les comptes Supabase Auth correspondants avec mots de passe simples.

- [ ] **Step 10.2 : Vérifier le trigger de liaison**

Les recipients `member-a@test.com` et `member-b@test.com` doivent maintenant avoir `auth_user_id` non NULL et `role = 'member'`.

- [ ] **Step 10.3 : Test isolation**

1. Lancer `pnpm dev`
2. Login en tant que `member-a@test.com` → `/admin/devices` doit afficher **2 devices** (ceux de Company A)
3. Se déconnecter
4. Login en tant que `member-b@test.com` → `/admin/devices` doit afficher **3 devices** (ceux de Company B)
5. Si l'un voit les devices de l'autre, RLS est mal configurée → corriger Task 3

- [ ] **Step 10.4 : Push CI + Netlify**

```bash
git push
```

Attendre la CI verte. Vérifier le déploiement Netlify : tester `/login` sur l'URL Netlify avec les mêmes comptes test.

- [ ] **Step 10.5 : Sauvegarder en mémoire**

Demander à l'agent de sauvegarder dans la mémoire :
- Les 2 comptes test de companies différentes (pour réutilisation future en E2E)
- Le fait que P2 est terminé

---

## Self-Review

- Spec section 6 (auth & multi-tenant) : ✓ Tasks 1-3 (schéma + trigger + RLS), Tasks 5-9 (côté Vue)
- Spec section 4 (architecture) : ✓ partiellement (Edge Functions arrivent en P3)
- Spec section 5 (structure de code) : ✓ stores/auth, lib/supabase typé, views/auth, views/admin, composables, types
- Spec section 8 (errors) : ✓ loading + error states dans authStore et useDevices, mais ErrorBoundary global déféré à P3
- Pas de placeholders ; tous les SQL et code TypeScript sont complets
- Types `Recipient` et `Device` viennent de `types/supabase.ts` généré → pas d'incohérence
- Commits fréquents (10 commits prévus)
